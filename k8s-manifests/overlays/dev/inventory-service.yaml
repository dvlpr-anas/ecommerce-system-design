apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  labels: { app: inventory-service, tier: backend }
spec:
  replicas: 1
  selector:
    matchLabels: { app: inventory-service }
  template:
    metadata:
      labels: { app: inventory-service, tier: backend }
    spec:
      containers:
        - name: inventory-service
          image: inventory-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 8086 }
          envFrom:
            - configMapRef: { name: sol-arch-env }
          env:
            - { name: PORT, value: "8086" }
          readinessProbe:
            httpGet: { path: /readyz, port: http }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
spec:
  selector: { app: inventory-service }
  ports:
    - { name: http, port: 8086, targetPort: http }
